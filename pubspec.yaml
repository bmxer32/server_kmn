name: server
description: KNB multiplayer WebSocket server.
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  shelf: ^1.4.0
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
